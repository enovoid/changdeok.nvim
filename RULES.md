# RULES.md — changdeok.nvim 작업 규칙

이 파일을 저장소 루트에 두고, Claude Code 나 다른 AI 에게 이 프로젝트 작업을
시킬 때마다 첫 메시지에서 읽게 한다.

> `RULES.md` 를 먼저 읽고, 거기 규칙을 지키면서 작업해줘.

여러 세션에 걸쳐 작업하다 보면 원래 판단이 조용히 뒤집힌다. 이 파일은 그걸
막기 위한 것이다. 규칙을 어겨야 할 이유가 생기면 **먼저 말하고 확인받는다.**

---

## 1. 색을 고르는 규칙

**오방색 원색은 구문 강조에 쓰지 않는다.**
청·적·황·백·흑은 방위와 상징을 멀리서 읽히게 하려고 채도를 끝까지 올린 의례용
팔레트다. 색동과 궁궐 외부 단청처럼 원거리 시인성이 목적인 곳에 쓰였지, 30cm
앞에서 여덟 시간 보라고 만든 색이 아니다. 이게 이 프로젝트의 출발점이고, 이걸
어기면 프로젝트의 이유가 사라진다.

**대신 오간색과 광물 안료의 탁도를 쓴다.**
녹·벽·홍·유황·자, 그리고 실제 안료가 가진 탁함. 뇌록은 회색이 섞인 청록이고,
석간주는 붉은 갈색 흙빛이며, 삼청은 형광 파랑이 아니라 가라앉은 남색이다.

**채도가 돌아오는 곳은 진단 메시지 하나뿐이다.**
그것도 배경 블록이 아니라 `undercurl` 로 칠한다. 배경 블록은 코드를 가린다.
"에러가 눈에 잘 띄어야 한다"는 이유로 배경 블록을 넣지 마라.

**새 색을 추가할 때 자문할 것:** 이 색을 여덟 시간 동안 30cm 앞에서 봐도
괜찮은가. 아니면 채도를 낮춘다.

---

## 2. 매핑은 고정이다

변형이 바뀌어도 안료와 구문의 대응은 유지한다. 색이 바뀌어도 코드를 읽는 습관은
바뀌지 않아야 한다.

| 안료 | 구문 | 이유 |
|---|---|---|
| 진사 | keyword, return | 기둥. 구조를 지탱하는 색 |
| 석황 | function, heading | 금박 자리에 쓰인 강조색 |
| 뇌록 | string | 단청 바탕칠. 가장 넓은 면적 |
| 삼청 | type, class | 가라앉은 남동석 청색 |
| 자주 | constant, number | 오간색 |
| 벽 | escape, property | 오간색 |
| 홍 | parameter, tag | 오간색 |

이 매핑에는 이유가 있다. 바꾸려면 이유를 대고 확인받는다.

---

## 3. 변형은 실제로 다른 장소여야 한다

밝기만 뒤집어서 라이트 테마를 만들지 않는다. 낙선재가 밝은 이유는 명도를
반전시켜서가 아니라, **애초에 단청을 하지 않은 백골집**이라서다. 안료 대신
재료(소나무 결·한지·황토·전돌)의 색으로 짰다.

새 변형을 추가하려면 창덕궁 안에서 색 처리가 실제로 다른 장소를 찾아야 한다.
근거가 없으면 추가하지 않는다. 변형 개수를 늘리는 것 자체는 가치가 아니다.

---

## 4. 대비는 측정한다

색을 눈대중으로 확정하지 않는다. `scripts/contrast_check.py` 가 게이트다.

- 본문 **7:1** (WCAG AAA)
- 구문·UI 텍스트 **4.5:1**
- 선택 영역 **3:1**
- 창 경계·들여쓰기 가이드 **1.4:1** — 정보를 전달하지 않는 장식 요소다.
  WCAG 3:1 은 의미를 가진 UI 컴포넌트 기준이라, 여기 적용하면 분할선이 본문보다
  시끄러워진다. 이 예외는 스크립트 주석에 이유가 남아 있다. 지우지 마라.

**`bg` 하나만 보지 않는다.** `bg`, `bg_dim`, `bg_float`, `bg_line` 네 배경 전부에
대해 계산하고 **가장 불리한 쌍**으로 판정한다. 팝업이나 사이드바 위에서 대비가
깨지는 게 실제로 가장 흔한 실패 지점이다.

**미달이면 명도만 조정한다.** 색상(hue)과 채도는 건드리지 않는다. 안료의 정체성은
hue 에 있다. hue 를 바꿔 통과시키는 건 통과가 아니라 다른 색으로 바꾼 것이다.

기준을 낮춰서 통과시키지 않는다. 기준을 바꿔야 한다면 먼저 말한다.

---

## 5. 정직성

**hex 값은 실측이 아니라 해석이다.**
건물 간의 색 처리 차이(인정전은 모로단청, 낙선재는 백골집)는 사실에 근거한다.
하지만 개별 hex 값은 그 성격을 화면용으로 옮긴 해석이다. 문화재청 실측 자료를
가져온 게 아니다.

README 나 커밋 메시지에서 **고증된 값인 것처럼 쓰지 마라.** "창덕궁의 실제
색" 같은 표현은 쓰지 않는다. "그곳의 색 처리에서 뽑은 해석"이 정확한 표현이다.

**출처를 감추지 않는다.**
대비 게이트를 빌드에 박는 방식은 dancheong.nvim 에서 영향받았다. 소재도 단청으로
겹친다. README 의 출처 표기를 지우거나 축소하지 마라.

**모르면 물어본다.** 안료의 실제 색감처럼 확신이 안 서는 부분은 추측해서 넘어가지
말고 확인을 요청한다.

---

## 6. 구조

```
lua/changdeok/palette.lua      색의 유일한 출처
lua/changdeok/highlights.lua   그룹 매핑. 여기에 hex 를 직접 쓰지 않는다
lua/changdeok/init.lua         setup / load / cycle
colors/changdeok-*.lua         진입점. 한 줄
scripts/contrast_check.py      게이트
```

**hex 는 `palette.lua` 에만 존재한다.** `highlights.lua` 에 하드코딩된 색이
들어가면 검증을 우회하게 된다. 이건 규칙 4 를 무력화하는 것이므로 하지 마라.

**주석에 근거를 남긴다.** 왜 이 안료가 이 구문에 붙었는지, 왜 이 예외를 뒀는지.
6개월 뒤에 읽을 사람을 위한 것이다.

---

## 7. 검증

작업이 끝났다고 말하기 전에 실제로 돌린다. "파일을 만들었다"와 "동작한다"는
다르다.

```sh
python3 scripts/contrast_check.py                 # 종료 코드 0
luac -p lua/changdeok/*.lua colors/*.lua          # 문법 (luac 이 없으면 건너뛴다)
nvim --headless --clean --cmd "set rtp+=$PWD" \
     -l scripts/load_check.lua                    # 세 변형 로드. 정상 0 / 실패 1
```

**세 번째 명령을 `nvim --headless "+colorscheme ..." +qa` 로 되돌리지 마라.**
그 형태는 존재하지 않는 colorscheme 을 부르든 lua 문법 에러가 나든 **항상 종료
코드 0** 을 돌려준다. 무엇이 깨져도 통과하는 가짜 검사가 된다. 확인한 사실이다.
`scripts/load_check.lua` 는 `pcall` 로 감싸고 직접 `os.exit(1)` 을 부른다.

`luac` 은 이 머신에 없다. 없으면 건너뛰어도 되는데, 문법 에러는 위 로드 검증이
어차피 잡기 때문이다.

실패하면 원인을 짚고 멈춘다. 우회하거나 조용히 넘어가지 않는다.

---

## 8. 커밋

Conventional Commits 를 쓴다. 팔레트 변경, 하이라이트 그룹, 검증 스크립트는
분리된 커밋으로.

되돌리기 어려운 동작(force push, public 저장소 생성, 기존 nvim 설정 파일 수정)은
실행 전에 확인받는다.

---

## 흔한 실패 (하지 말 것)

- 유명 테마에서 본 hex 를 가져와 "한국 색"이라고 붙이기
- 진단 메시지에 배경 블록 넣기
- 라이트 변형을 다크의 명도 반전으로 만들기
- 대비 미달을 hue 변경으로 통과시키기
- `highlights.lua` 에 hex 직접 쓰기
- 검증 스크립트를 돌리지 않고 "통과했다"고 보고하기
- 근거 없이 변형 개수 늘리기
- 실측인 것처럼 서술하기
- 이전 세션의 판단을 말없이 뒤집기
